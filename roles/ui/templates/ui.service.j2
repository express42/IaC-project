[Unit]
Description=Puma HTTP Server
After=network.target

[Service]
Type=simple
User={{ host_user }}
WorkingDirectory=/home/ubuntu/reddit/ui
EnvironmentFile=/home/ubuntu/reddit/ui/ui_config
ExecStart=/bin/bash -lc 'puma'
Restart=always

[Install]
WantedBy=multi-user.target
